apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: production-user-service
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: 'https://github.com/KeremAR/proxmox-k3s'
    path: helm-charts/user-service
    targetRevision: 'v1.0.5'
    helm:
      valueFiles:
        - values.yaml
        - values-prod.yaml
      parameters:
        # Image tag (managed by production deployment process)
        - name: image.tag
          value: 'd40d855'
  destination:
    server: https://kubernetes.default.svc
    namespace: production
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
